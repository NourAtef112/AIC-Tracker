# MTC-AIC4 project dependencies
# RTX 3060 / CUDA 13.2 — uses PyTorch cu128 wheels (backward compatible)
#
# Install with:
#   pip install -r requirements.txt
#
# Note: OSTrack and SGLATrack each run in their own conda env.
# This file covers shared utilities, root-level inference scripts, and
# all model library dependencies (lib/ internals for both models).

--extra-index-url https://download.pytorch.org/whl/cu128

# ── Core inference & shared utilities ────────────────────────────────────────
torch==2.11.0+cu128
torchvision==0.26.0+cu128
opencv-python==4.13.0.92
numpy>=2.4.3
thop==0.1.1.post2209072238
onnxruntime-gpu==1.25.0
pandas>=2.0.0

# ── Model library training dependencies (OSTrack / SGLATrack lib/) ────────────
timm>=0.9.0
PyYAML>=6.0
easydict>=1.9
lmdb>=1.4.0
scipy>=1.10.0
pycocotools>=2.0.6
jpeg4py
wandb>=0.15.0
tensorboard>=2.13.0
tensorboardX>=2.6
visdom>=0.2.4
tqdm>=4.65.0
colorama>=0.4.6

# ── Build tools (install before pycocotools) ──────────────────────────────────
cython>=3.0
