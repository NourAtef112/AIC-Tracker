# MTC-AIC4 project dependencies
# RTX 3060 / CUDA 13.2 — uses PyTorch cu128 wheels (backward compatible)
#
# Install with:
#   pip install -r requirements.txt
#
# Note: OSTrack and LightTrack each run in their own conda env (Python 3.8 / 3.7).
# This file covers the shared utilities and the root-level inference scripts.

--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.11.0+cu128
torchvision==0.26.0+cu128
opencv-python==4.13.0.92
numpy>=2.4.3
thop==0.1.1.post2209072238
onnxruntime-gpu==1.25.0
